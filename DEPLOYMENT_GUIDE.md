# 网页发布指南

本文档介绍如何将 Dream Painter（梦境画师）Web 应用发布到生产环境。

## 📋 目录

1. [发布前准备](#发布前准备)
2. [构建生产版本](#构建生产版本)
3. [部署平台选择](#部署平台选择)
4. [详细部署步骤](#详细部署步骤)
5. [环境变量配置](#环境变量配置)
6. [常见问题](#常见问题)

---

## 🚀 发布前准备

### 1. 检查项目状态

确保项目可以正常运行：

```bash
# 进入项目目录
cd "第2阶段-代码开发与迭代/产出文档/code_files"

# 安装依赖（如果还没有安装）
npm install

# 本地测试运行
npm run dev
```

### 2. 检查 API 配置

确保 `src/api/coze.js` 中的 API 配置正确：
- API Token
- Bot ID
- API 端点地址

---

## 🔨 构建生产版本

### 步骤 1：构建项目

```bash
# 在项目根目录执行
npm run build
```

构建完成后，会在项目根目录生成 `dist` 文件夹，包含所有优化后的静态文件。

### 步骤 2：预览构建结果（可选）

```bash
# 预览生产版本
npm run preview
```

在浏览器中打开显示的地址（通常是 `http://localhost:4173`），检查构建结果是否正确。

---

## 🌐 部署平台选择

### 推荐平台对比

| 平台 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **Vercel** | 自动部署、CDN、免费SSL | 需要Git仓库 | 推荐首选 |
| **Netlify** | 简单易用、免费SSL | 构建时间限制 | 适合小型项目 |
| **GitHub Pages** | 完全免费、Git集成 | 需要Git仓库 | 开源项目 |
| **Cloudflare Pages** | 全球CDN、快速 | 配置稍复杂 | 需要全球加速 |

---

## 📝 详细部署步骤

### 方案一：Vercel 部署（推荐）

#### 1. 准备工作

- 注册 [Vercel](https://vercel.com) 账号
- 将代码推送到 GitHub/GitLab/Bitbucket

#### 2. 部署步骤

**方法 A：通过 Vercel 网站**

1. 登录 [Vercel Dashboard](https://vercel.com/dashboard)
2. 点击 "Add New Project"
3. 导入你的 Git 仓库
4. 配置项目：
   - **Framework Preset**: Vite
   - **Root Directory**: `第2阶段-代码开发与迭代/产出文档/code_files`（或调整路径）
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
5. 添加环境变量（如果需要，见下方"环境变量配置"）
6. 点击 "Deploy"

**方法 B：通过 Vercel CLI**

```bash
# 安装 Vercel CLI
npm i -g vercel

# 在项目目录执行
cd "第2阶段-代码开发与迭代/产出文档/code_files"
vercel

# 按照提示完成部署
```

#### 3. 自定义域名（可选）

1. 在 Vercel 项目设置中添加自定义域名
2. 按照提示配置 DNS 记录
3. 自动获得免费 SSL 证书

---

### 方案二：Netlify 部署

#### 1. 准备工作

- 注册 [Netlify](https://www.netlify.com) 账号
- 将代码推送到 Git 仓库

#### 2. 部署步骤

**方法 A：通过 Netlify 网站**

1. 登录 [Netlify Dashboard](https://app.netlify.com)
2. 点击 "Add new site" → "Import an existing project"
3. 连接 Git 仓库
4. 配置构建设置：
   - **Base directory**: `第2阶段-代码开发与迭代/产出文档/code_files`
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
5. 添加环境变量（如果需要）
6. 点击 "Deploy site"

**方法 B：拖拽部署**

1. 在项目目录执行 `npm run build`
2. 登录 Netlify Dashboard
3. 将 `dist` 文件夹拖拽到 Netlify 部署区域

**方法 C：通过 Netlify CLI**

```bash
# 安装 Netlify CLI
npm i -g netlify-cli

# 在项目目录执行
cd "第2阶段-代码开发与迭代/产出文档/code_files"
npm run build
netlify deploy --prod --dir=dist
```

---

### 方案三：GitHub Pages 部署

#### 1. 准备工作

- 将代码推送到 GitHub 仓库
- 确保仓库是公开的（免费版）

#### 2. 安装 gh-pages

```bash
cd "第2阶段-代码开发与迭代/产出文档/code_files"
npm install --save-dev gh-pages
```

#### 3. 修改 package.json

在 `package.json` 的 `scripts` 中添加：

```json
{
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

#### 4. 修改 vite.config.js

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/your-repo-name/', // 替换为你的仓库名称
  server: {
    port: 3000,
    open: true
  },
  build: {
    outDir: 'dist',
    sourcemap: true
  }
})
```

#### 5. 部署

```bash
npm run deploy
```

#### 6. 启用 GitHub Pages

1. 在 GitHub 仓库设置中，进入 "Pages"
2. 选择 Source: `gh-pages` 分支
3. 保存设置
4. 访问：`https://your-username.github.io/your-repo-name/`

---

### 方案四：Cloudflare Pages 部署

#### 1. 准备工作

- 注册 [Cloudflare](https://www.cloudflare.com) 账号
- 将代码推送到 Git 仓库

#### 2. 部署步骤

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com)
2. 进入 "Pages" → "Create a project"
3. 连接 Git 仓库
4. 配置构建设置：
   - **Framework preset**: Vite
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
   - **Root directory**: `第2阶段-代码开发与迭代/产出文档/code_files`
5. 添加环境变量（如果需要）
6. 点击 "Save and Deploy"

---

## 🔐 环境变量配置

### 如果使用环境变量

如果你的项目使用了环境变量（如 API 密钥），需要配置：

#### Vercel

1. 项目设置 → Environment Variables
2. 添加变量：
   - `VITE_API_TOKEN`
   - `VITE_BOT_ID`
   - 等

#### Netlify

1. Site settings → Environment variables
2. 添加变量（同上）

#### 本地开发

创建 `.env` 文件（不要提交到 Git）：

```env
VITE_API_TOKEN=your_token_here
VITE_BOT_ID=your_bot_id_here
```

在代码中使用：

```javascript
const API_TOKEN = import.meta.env.VITE_API_TOKEN
```

**注意**：Vite 要求环境变量以 `VITE_` 开头才能在客户端使用。

---

## ⚠️ 常见问题

### 1. 构建失败

**问题**：`npm run build` 报错

**解决方案**：
- 检查 Node.js 版本（建议 18+）
- 删除 `node_modules` 和 `package-lock.json`，重新安装：
  ```bash
  rm -rf node_modules package-lock.json
  npm install
  ```

### 2. 路由 404 错误

**问题**：刷新页面后出现 404

**解决方案**：
- Vercel/Netlify：会自动处理，无需配置
- GitHub Pages：需要配置重定向（创建 `public/_redirects` 文件）

### 3. API 请求失败

**问题**：部署后 API 调用失败

**解决方案**：
- 检查 CORS 配置
- 确认 API 端点地址正确
- 检查环境变量是否配置

### 4. 静态资源加载失败

**问题**：图片、字体等资源无法加载

**解决方案**：
- 检查 `vite.config.js` 中的 `base` 配置
- 确保资源路径使用相对路径或正确的 base URL

### 5. 构建文件过大

**问题**：`dist` 文件夹过大

**解决方案**：
- 检查是否有未使用的依赖
- 使用代码分割
- 压缩图片资源

---

## 📊 部署后检查清单

- [ ] 网站可以正常访问
- [ ] 所有页面功能正常
- [ ] API 调用成功
- [ ] 响应式设计在移动端正常
- [ ] 加载速度正常
- [ ] SSL 证书已启用（HTTPS）
- [ ] 自定义域名（如果使用）已配置

---

## 🔄 持续部署

### 自动部署设置

大多数平台支持自动部署：

1. **Vercel/Netlify**：连接 Git 仓库后自动部署
2. **GitHub Pages**：使用 GitHub Actions 自动部署

### 手动更新

```bash
# 构建新版本
npm run build

# 重新部署（根据平台选择）
# Vercel: vercel --prod
# Netlify: netlify deploy --prod --dir=dist
```

---

## 📚 相关资源

- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)
- [Vercel 文档](https://vercel.com/docs)
- [Netlify 文档](https://docs.netlify.com)
- [GitHub Pages 文档](https://docs.github.com/pages)

---

## 💡 推荐方案

**对于本项目，推荐使用 Vercel**，因为：
1. ✅ 部署简单，支持自动部署
2. ✅ 全球 CDN，访问速度快
3. ✅ 免费 SSL 证书
4. ✅ 与 Git 集成良好
5. ✅ 支持环境变量
6. ✅ 免费额度充足

---

## 🎉 完成

部署完成后，你的网站就可以通过生成的 URL 访问了！

如有问题，请查看对应平台的文档或联系技术支持。

